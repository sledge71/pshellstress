# Einfacher Aufruf
curl -I https://www.google.com

# Aufruf mit Header und Timeouts
curl -X GET https://httpstat.us/200 -H "Accept: application/json" --connect-timeout 5

# POST Request mit JSON Daten (Wichtig: Innere Anführungszeichen escapen \" oder einfache ' nutzen, wenn möglich)
curl -X POST https://httpbin.org/post -H "Content-Type: application/json" -d "{\"name\": \"test\", \"active\": true}"

# Fehlerhafter Aufruf zum Testen
curl https://domain.die.es.nicht.gibt.de